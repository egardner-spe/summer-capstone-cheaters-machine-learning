# Week 1-2 (data, EDA, features)
numpy>=1.24
pandas>=2.0
pyarrow>=12.0          # parquet
matplotlib>=3.7
scipy>=1.10

# Week 3-6 (modelling, imbalance, interpretability) - installed now so the
# environment is ready; not yet used in code.
scikit-learn>=1.3
xgboost>=2.0
imbalanced-learn>=0.11
shap>=0.43

# Notebooks
jupyter>=1.0
notebook>=7.0
